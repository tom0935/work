<nav class="navbar navbar-default navbar-static-top navbar-inverse" role="navigation">
    <div class="navbar-header">
        <button type="button" class="navbar-toggle" data-toggle="collapse" data-target="#bs-example-navbar-collapse-1">
            <span class="sr-only">Toggle navigation</span> <span class="icon-bar"></span><span
                class="icon-bar"></span><span class="icon-bar"></span>
        </button>
        <a class="navbar-brand" href="@Request.ApplicationPath"><span class="glyphicon glyphicon-home">
        </span> Howard Taipei</a>
    </div>
    <div class="collapse navbar-collapse" id="bs-example-navbar-collapse-1">
        <ul class="nav navbar-nav">
            @if (User.Identity.IsAuthenticated)
            {
               @Html.Raw(ViewData["NavMenu"])
            }
        </ul>
        @if (User.Identity.IsAuthenticated)
        {
            //使用者功能選單
            <ul class="nav navbar-nav navbar-right" style="background-color: #357EBD;">
                <li class="dropdown"><a href="#" class="dropdown-toggle glyphicon glyphicon-cog"
                    data-toggle="dropdown" style="color: White;"> 使用者選單
                    <b class="caret"></b></a>
                    <ul class="dropdown-menu" role="menu">
                        <li role="presentation" class="dropdown-header"><span class="glyphicon glyphicon-user"></span> 帳號</li>
                        <li role="presentation">
                            <ul>
                            <li role="presentation">@User.Identity.Name</li>
                            </ul>
                        </li>
                        <li role="presentation" class="divider"></li>
                        <li role="presentation" class="dropdown-header"><span class="glyphicon glyphicon-list-alt"></span> 所屬部門</li>
                        <li role="presentation">
                            <ul>
                                @if (ViewData["DepartmentMsg"] != "" && ViewData["DepartmentMsg"] != null)
                                {
                                    foreach (string item in (List<string>)ViewData["DepartmentMsg"])
                                    {
                                    <li role="presentation">@Html.Raw(item)</li>
                                    }
                                }
                            </ul>
                        </li>
                        <li role="presentation" class="divider"></li>
                        <li>@Html.ActionLink(" 變更密碼", "ChangePwd", "User", new { id = "" }, new { @class = "glyphicon glyphicon-lock" })</li>
                        <li>@Html.ActionLink(" 登出", "Logout", "Home", new { id = "" }, new { @class = "glyphicon glyphicon-off" })</li>
                    </ul>
                </li>
            </ul>
        }
    </div>
</nav>